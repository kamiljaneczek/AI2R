type TQuestionData = {
  code: number;
  msg: string;
  cookie: string;
};

type TTokenData = {
  code: number;
  msg: string;
  token: string;
};

type TTAnswerData = {
  code: number;
  msg: string;
  note: string;
};
